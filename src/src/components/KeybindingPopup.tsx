import { FunctionComponent } from 'react';

const KeybindingPopup: FunctionComponent = () => {
	// TODO: rename?

	// TODO: close on keybinding or click outside of it
	// TODO: on click on one of the options, execute it

	return <div></div>; // TODO: should popup from below (as in Emacs) and show available options (based on argument) + add additional keybindings while opened or onClose hook
};

export default KeybindingPopup;
